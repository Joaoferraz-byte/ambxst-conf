# Retired shell integration repository

This repository is intentionally kept without active source code. The former shell integration was retired so that the desktop configuration can be rebuilt around explicit owners and documented application contracts.

No package, module, patch, palette bridge, activation script, test suite, or runtime service is provided here. Historical commits remain available for audit and rollback, but this repository must not be imported by the active NixOS configuration.
