diff --git a/assets/presets/Ambxst Default/workspaces.json b/assets/presets/Ambxst Default/workspaces.json
index fe34399..8d608d8 100644
--- a/assets/presets/Ambxst Default/workspaces.json
+++ b/assets/presets/Ambxst Default/workspaces.json
@@ -4 +4 @@
-    "showAppIcons": true,
+    "showAppIcons": false,
@@ -6 +6 @@
-    "shown": 10
+    "shown": 3
diff --git a/modules/bar/BarContent.qml b/modules/bar/BarContent.qml
index 9d43848..90ea040 100644
--- a/modules/bar/BarContent.qml
+++ b/modules/bar/BarContent.qml
@@ -395,6 +395,9 @@ Item {
-                        LayoutSelectorButton {
-                            id: layoutSelectorButton
-                            bar: root
-                            layerEnabled: root.shadowsEnabled
-                            startRadius: root.innerRadius
-                            endRadius: (root.pinButtonVisible) ? root.innerRadius : (root.dockAtStart ? root.innerRadius : root.outerRadius)
+                        Loader {
+                            id: layoutSelectorLoader
+                            active: AxctlService.compositorName !== "niri"
+                            sourceComponent: LayoutSelectorButton {
+                                bar: root
+                                layerEnabled: root.shadowsEnabled
+                                startRadius: root.innerRadius
+                                endRadius: (root.pinButtonVisible) ? root.innerRadius : (root.dockAtStart ? root.innerRadius : root.outerRadius)
+                            }
@@ -636,4 +639,3 @@ Item {
-                                LayoutSelectorButton {
-                                    id: layoutSelectorButtonVert
-                                    bar: root
-                                    layerEnabled: root.shadowsEnabled
+                                Loader {
+                                    id: layoutSelectorLoaderVert
+                                    active: AxctlService.compositorName !== "niri"
@@ -641,3 +643,7 @@ Item {
-                                    startRadius: root.outerRadius
-                                    endRadius: root.innerRadius
-                                    vertical: true
+                                    sourceComponent: LayoutSelectorButton {
+                                        bar: root
+                                        layerEnabled: root.shadowsEnabled
+                                        startRadius: root.outerRadius
+                                        endRadius: root.innerRadius
+                                        vertical: true
+                                    }
diff --git a/modules/bar/systray/SysTray.qml b/modules/bar/systray/SysTray.qml
index 83464b5..75526b8 100644
--- a/modules/bar/systray/SysTray.qml
+++ b/modules/bar/systray/SysTray.qml
@@ -11,2 +11,31 @@ import qs.modules.components
-    // Hide when no tray items
-    visible: hasItems
+    // Hide when no tray items. Easy Effects, network and audio controls are
+    // rendered by Ambxst widgets, so only exact SNI identities are filtered.
+    visible: visibleItems.length > 0
+
+    readonly property var hiddenItemTokens: [
+        "nm-applet",
+        "nm-connection-editor",
+        "networkmanager",
+        "blueman-applet",
+        "blueman-manager",
+        "bluetooth",
+        "com.github.wwmm.easyeffects",
+        "easyeffects",
+        "easy-effects",
+        "easy effects",
+        "easy_effects",
+        "pasystray",
+        "volumeicon",
+        "wireplumber"
+    ]
+
+    function isShellManaged(item) {
+        const fields = [item.id, item.title, item.tooltipTitle, item.icon]
+            .filter(value => value !== undefined && value !== null)
+            .map(value => String(value).toLowerCase());
+        return fields.some(field => hiddenItemTokens.some(token =>
+            field === token || field.endsWith("." + token) || field.endsWith("/" + token) ||
+            field.endsWith("-" + token) || field.endsWith("_" + token)));
+    }
+
+    readonly property var visibleItems: SystemTray.items.values.filter(item => !isShellManaged(item))
@@ -29 +58 @@ import qs.modules.components
-    readonly property bool hasItems: rowRepeater.count > 0 || columnRepeater.count > 0
+    readonly property bool hasItems: visibleItems.length > 0
@@ -46 +75 @@ import qs.modules.components
-            model: SystemTray.items
+            model: root.visibleItems
@@ -65 +94 @@ import qs.modules.components
-            model: SystemTray.items
+            model: root.visibleItems
diff --git a/modules/bar/workspaces/Workspaces.qml b/modules/bar/workspaces/Workspaces.qml
index 5a1625c..e0c8e65 100644
--- a/modules/bar/workspaces/Workspaces.qml
+++ b/modules/bar/workspaces/Workspaces.qml
@@ -474,5 +474,4 @@ Item {
-                    property var mainAppIconSource: {
-                        if (focusedDesktopEntry && focusedDesktopEntry.icon) {
-                            return Quickshell.iconPath(focusedDesktopEntry.icon, "image-missing");
-                        }
-                        return Quickshell.iconPath(AppSearch.getCachedIcon(focusedWindow ? focusedWindow.class : undefined), "image-missing");
+                    property string mainAppIconName: {
+                        if (focusedDesktopEntry && focusedDesktopEntry.icon)
+                            return focusedDesktopEntry.icon;
+                        return AppSearch.guessIcon(focusedWindow ? focusedWindow.class : "");
@@ -525 +524 @@ Item {
-                        IconImage {
+                        Image {
@@ -527,36 +526,6 @@ Item {
-                            anchors.bottom: parent.bottom
-                            anchors.right: parent.right
-                            anchors.bottomMargin: (!Config.workspaces.alwaysShowNumbers && Config.workspaces.showAppIcons) ? Math.round((workspaceButtonWidth - workspaceIconSize) / 2) : workspaceIconMarginShrinked
-                            anchors.rightMargin: (!Config.workspaces.alwaysShowNumbers && Config.workspaces.showAppIcons) ? Math.round((workspaceButtonWidth - workspaceIconSize) / 2) : workspaceIconMarginShrinked
-
-                            source: workspaceButtonBackground.mainAppIconSource
-                            implicitSize: (!Config.workspaces.alwaysShowNumbers && Config.workspaces.showAppIcons) ? workspaceIconSize : workspaceIconSizeShrinked
-
-                            Behavior on opacity {
-                                enabled: Config.animDuration > 0
-                                NumberAnimation {
-                                    duration: 150
-                                    easing.type: Easing.OutQuad
-                                }
-                            }
-                            Behavior on anchors.bottomMargin {
-                                enabled: Config.animDuration > 0
-                                NumberAnimation {
-                                    duration: 150
-                                    easing.type: Easing.OutQuad
-                                }
-                            }
-                            Behavior on anchors.rightMargin {
-                                enabled: Config.animDuration > 0
-                                NumberAnimation {
-                                    duration: 150
-                                    easing.type: Easing.OutQuad
-                                }
-                            }
-                            Behavior on implicitSize {
-                                enabled: Config.animDuration > 0
-                                NumberAnimation {
-                                    duration: 150
-                                    easing.type: Easing.OutQuad
-                                }
-                            }
+                            anchors.fill: parent
+                            source: "image://icon/" + workspaceButtonBackground.mainAppIconName
+                            sourceSize.width: width * 2
+                            sourceSize.height: height * 2
+                            fillMode: Image.PreserveAspectFit
+                            mipmap: true
@@ -611,5 +580,4 @@ Item {
-                    property var mainAppIconSource: {
-                        if (focusedDesktopEntry && focusedDesktopEntry.icon) {
-                            return Quickshell.iconPath(focusedDesktopEntry.icon, "image-missing");
-                        }
-                        return Quickshell.iconPath(AppSearch.getCachedIcon(focusedWindow ? focusedWindow.class : undefined), "image-missing");
+                    property string mainAppIconName: {
+                        if (focusedDesktopEntry && focusedDesktopEntry.icon)
+                            return focusedDesktopEntry.icon;
+                        return AppSearch.guessIcon(focusedWindow ? focusedWindow.class : "");
@@ -662 +630 @@ Item {
-                        IconImage {
+                        Image {
@@ -664,36 +632,6 @@ Item {
-                            anchors.bottom: parent.bottom
-                            anchors.right: parent.right
-                            anchors.bottomMargin: (!Config.workspaces.alwaysShowNumbers && Config.workspaces.showAppIcons) ? Math.round((workspaceButtonWidth - workspaceIconSize) / 2) : workspaceIconMarginShrinked
-                            anchors.rightMargin: (!Config.workspaces.alwaysShowNumbers && Config.workspaces.showAppIcons) ? Math.round((workspaceButtonWidth - workspaceIconSize) / 2) : workspaceIconMarginShrinked
-
-                            source: workspaceButtonBackgroundVert.mainAppIconSource
-                            implicitSize: (!Config.workspaces.alwaysShowNumbers && Config.workspaces.showAppIcons) ? workspaceIconSize : workspaceIconSizeShrinked
-
-                            Behavior on opacity {
-                                enabled: Config.animDuration > 0
-                                NumberAnimation {
-                                    duration: 150
-                                    easing.type: Easing.OutQuad
-                                }
-                            }
-                            Behavior on anchors.bottomMargin {
-                                enabled: Config.animDuration > 0
-                                NumberAnimation {
-                                    duration: 150
-                                    easing.type: Easing.OutQuad
-                                }
-                            }
-                            Behavior on anchors.rightMargin {
-                                enabled: Config.animDuration > 0
-                                NumberAnimation {
-                                    duration: 150
-                                    easing.type: Easing.OutQuad
-                                }
-                            }
-                            Behavior on implicitSize {
-                                enabled: Config.animDuration > 0
-                                NumberAnimation {
-                                    duration: 150
-                                    easing.type: Easing.OutQuad
-                                }
-                            }
+                            anchors.fill: parent
+                            source: "image://icon/" + workspaceButtonBackgroundVert.mainAppIconName
+                            sourceSize.width: width * 2
+                            sourceSize.height: height * 2
+                            fillMode: Image.PreserveAspectFit
+                            mipmap: true
diff --git a/modules/dock/DockAppButton.qml b/modules/dock/DockAppButton.qml
index 3132417..4240962 100644
--- a/modules/dock/DockAppButton.qml
+++ b/modules/dock/DockAppButton.qml
@@ -112,0 +113,3 @@ Button {
+                        // Dock icons are tinted by default; callers may still
+                        // disable tint globally for other shell surfaces.
+                        active: true
diff --git a/modules/services/GlobalShortcuts.qml b/modules/services/GlobalShortcuts.qml
index b1aa3be..a33e9fc 100644
--- a/modules/services/GlobalShortcuts.qml
+++ b/modules/services/GlobalShortcuts.qml
@@ -81,0 +82,10 @@ QtObject {
+            case "ocr":
+                Screenshot.initialize();
+                Screenshot.captureMode = "ocr";
+                GlobalStates.screenshotToolVisible = true;
+                break;
+            case "qr":
+                Screenshot.initialize();
+                Screenshot.captureMode = "qr";
+                GlobalStates.screenshotToolVisible = true;
+                break;
diff --git a/modules/shell/ReservationWindows.qml b/modules/shell/ReservationWindows.qml
index 26da91a..ff434f2 100644
--- a/modules/shell/ReservationWindows.qml
+++ b/modules/shell/ReservationWindows.qml
@@ -22,0 +23 @@ Item {
+    property int dockWidth: 0
@@ -133 +134 @@ Item {
-            if (dockEnabled && dockPosition === "left" && dockPinned) zone += dockHeight;
+            if (dockEnabled && dockPosition === "left" && dockPinned) zone += dockWidth;
@@ -169 +170 @@ Item {
-            if (dockEnabled && dockPosition === "right" && dockPinned) zone += dockHeight;
+            if (dockEnabled && dockPosition === "right" && dockPinned) zone += dockWidth;
diff --git a/modules/shell/UnifiedShellPanel.qml b/modules/shell/UnifiedShellPanel.qml
index 028c2dc..c4a90e4 100644
--- a/modules/shell/UnifiedShellPanel.qml
+++ b/modules/shell/UnifiedShellPanel.qml
@@ -77 +77,2 @@ PanelWindow {
-    readonly property int dockHeight: dockContent.dockSize + dockContent.totalMargin
+    readonly property int dockHeight: dockContent.isVertical ? 0 : dockContent.dockSize + dockContent.totalMargin
+    readonly property int dockWidth: dockContent.isVertical ? dockContent.dockSize + dockContent.totalMargin : 0
diff --git a/shell.qml b/shell.qml
index 888996d..b78cedb 100644
--- a/shell.qml
+++ b/shell.qml
@@ -112,0 +113 @@ ShellRoot {
+                dockWidth: unifiedPanel.dockWidth
